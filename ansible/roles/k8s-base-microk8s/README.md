# MicroK8s

[Microk8s](https://canonical.com/microk8s) is a flavor of Kubernetes (k8s). The `k8s-base-microk8s` role sets up microk8s on the target host. This role can also uninstall microk8s by setting the `uninstall` variable to `true`.

## Getting Started

This role works out of the box.
