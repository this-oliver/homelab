# Helm

This role sets up, [helm](https://helm.sh/docs/), a sort of package manager for Kubernetes.

## Getting Started

This role works out of the box.

## Usage

To get started with `helm`, run the following command in your target host:

```bash
helm --help
```
