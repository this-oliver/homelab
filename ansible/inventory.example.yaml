controllers:
  hosts:
    one.example.com:
      ansible_user: foobar
      ansible_port: 22
      ansible_ssh_private_key_file: /path/to/key
  vars:
    node_type: "controller"

workers:
  hosts:
    two.example.com:
      ansible_user: foobar
      ansible_port: 22
      ansible_ssh_private_key_file: /path/to/key
  vars:
    node_type: "workers"
