# Structural vars. User-configurable vars are declared in vars/main.yaml (top-level).
helm_release:
  name: metallb
  namespace: metallb-system
  chart:
    ref: metallb/metallb
    version: "0.16.1"
  repo:
    name: metallb
    url: https://metallb.github.io/metallb
