# Kubectl

This role sets up, [kubectl](https://kubernetes.io/docs/tasks/tools/#kubectl), the official Kubernetes command-line tool.

## Getting Started

This role works out of the box.

## Usage

To get started with `kubectl`, run the following command in your target host:

```bash
kubectl --help
```
